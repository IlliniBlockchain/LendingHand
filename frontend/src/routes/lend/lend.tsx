import React from "react";

interface lendProps {}

export const Lend: React.FC<lendProps> = ({}) => {
  return (
    <>
      this is the lending page, render the cards and filter components in this
      route.
    </>
  );
};
